# Fast local image: run `mvn -DskipTests package` first.
FROM eclipse-temurin:21-jre-jammy
WORKDIR /app
COPY target/identityservice-0.0.1-SNAPSHOT.jar app.jar
EXPOSE 8080
ENV SPRING_PROFILES_ACTIVE=docker
ENTRYPOINT ["java", "-jar", "app.jar"]
