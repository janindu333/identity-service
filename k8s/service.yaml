apiVersion: v1
kind: Service
metadata:
  name: identity-service
  namespace: saloon-system
  labels:
    app: identity-service
spec:
  type: ClusterIP
  selector:
    app: identity-service
  ports:
    - name: http
      port: 8082
      targetPort: 8082
